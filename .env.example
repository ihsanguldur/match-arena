# Copy to .env and fill in a real value before running `docker compose up`.
# Must be >= 256 bits (32+ bytes) or account-service's JWT signing will fail
# with WeakKeyException. Generate one with: openssl rand -base64 48
JWT_SECRET=
